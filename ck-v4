#!/bin/bash
# ck-v4 - C-Kernel-Engine v4 CLI wrapper
# Usage: ./ck-v4 run HuggingFaceTB/SmolLM-135M

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/scripts/ck_run_v4.py" "$@"
